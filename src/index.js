import React from "react";
import ReactDOM from "react-dom";
import { BrowserRouter } from "react-router-dom";
import './App';

const root = ReactDOM.creatRoot(document.getElementsById('root'));
root.render(
    <BrowserRouter>
        <App />
    </BrowserRouter>
  );
  

